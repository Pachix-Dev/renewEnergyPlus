<section class='text-white py-12 px-6'>
  <div class='max-w-6xl mx-auto flex flex-wrap gap-6 justify-center'>
    <!-- Card 1 -->
    <div
      class='relative rounded-2xl overflow-hidden shadow-lg'
      data-aos='fade-up'
      data-aos-duration='1000'
    >
      <!-- Imagen de fondo -->
      <video
        class='absolute inset-0 object-cover w-full h-full'
        autoplay
        loop
        muted
      >
        <source src='/video/segmentos1.mp4' type='video/mp4' />
      </video>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/10'
      >
      </div>

      <!-- Contenido -->
      <div
        class='relative bottom-0 p-6 flex flex-col justify-between h-full text-white'
      >
        <div>
          <h3 class='py-10 text-2xl font-extrabold'>
            Sistemas de tecnología fotovoltaica
          </h3>
        </div>
      </div>
    </div>

    <!-- Card 2 -->
    <div
      class='relative rounded-2xl overflow-hidden shadow-lg'
      data-aos='fade-up'
      data-aos-duration='1200'
    >
      <!-- Imagen de fondo -->
      <video
        class='absolute inset-0 object-cover w-full h-full'
        autoplay
        loop
        muted
      >
        <source src='/video/segmentos7.mp4' type='video/mp4' />
      </video>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/10'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <h3 class='py-10 text-2xl font-extrabold'>Almacenamiento</h3>
        </div>
      </div>
    </div>

    <!-- Card 3 -->
    <div
      class='relative rounded-2xl overflow-hidden shadow-lg'
      data-aos='fade-up'
      data-aos-duration='1400'
    >
      <!-- Imagen de fondo -->
      <video
        class='absolute inset-0 object-cover w-full h-full'
        autoplay
        loop
        muted
      >
        <source src='/video/segmentos6.mp4' type='video/mp4' />
      </video>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/10'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <h3 class='py-10 text-2xl font-extrabold'>Componentes y equipo</h3>
        </div>
      </div>
    </div>

    <!-- Card 4 -->
    <div
      class='relative rounded-2xl overflow-hidden shadow-lg'
      data-aos='fade-up'
      data-aos-duration='1600'
    >
      <!-- Imagen de fondo -->
      <video
        class='absolute inset-0 object-cover w-full h-full'
        autoplay
        loop
        muted
      >
        <source src='/video/segmentos3.mp4' type='video/mp4' />
      </video>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/10'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <h3 class='py-10 text-2xl font-extrabold'>
            Tecnologías térmicas y solares
          </h3>
        </div>
      </div>
    </div>

    <!-- Card 5 -->
    <div
      class='relative rounded-2xl overflow-hidden shadow-lg'
      data-aos='fade-up'
      data-aos-duration='1800'
    >
      <!-- Imagen de fondo -->
      <video
        class='absolute inset-0 object-cover w-full h-full'
        autoplay
        loop
        muted
      >
        <source src='/video/segmentos4.mp4' type='video/mp4' />
      </video>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/10'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <h3 class='py-10 text-2xl font-extrabold'>Infraestructura de red</h3>
        </div>
      </div>
    </div>

    <!-- Card 6 -->
    <div
      class='relative rounded-2xl overflow-hidden shadow-lg'
      data-aos='fade-up'
      data-aos-duration='2000'
    >
      <!-- Imagen de fondo -->
      <video
        class='absolute inset-0 object-cover w-full h-full'
        autoplay
        loop
        muted
      >
        <source src='/video/segmentos5.mp4' type='video/mp4' />
      </video>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/10'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <h3 class='py-10 text-2xl font-extrabold'>
            Información, comunicación y servicios
          </h3>
        </div>
      </div>
    </div>

    <!-- Card 7 -->
    <div
      class='relative rounded-2xl overflow-hidden shadow-lg'
      data-aos='fade-up'
      data-aos-duration='2200'
    >
      <!-- Imagen de fondo -->
      <video
        class='absolute inset-0 object-cover w-full h-full'
        autoplay
        loop
        muted
      >
        <source src='/video/segmentos2.mp4' type='video/mp4' />
      </video>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/10'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <h3 class='py-10 text-2xl font-extrabold'>Eólico</h3>
        </div>
      </div>
    </div>

    <!-- Card 7 -->
    <div
      class='relative rounded-2xl overflow-hidden shadow-lg'
      data-aos='fade-up'
      data-aos-duration='2200'
    >
      <!-- Imagen de fondo -->
      <video
        class='absolute inset-0 object-cover w-full h-full'
        autoplay
        loop
        muted
      >
        <source src='/video/segmentos8.mp4' type='video/mp4' />
      </video>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/10'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <h3 class='py-10 text-2xl font-extrabold'>Hidrógeno verde</h3>
        </div>
      </div>
    </div>
  </div>
</section>
