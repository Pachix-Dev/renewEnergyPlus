<section class='bg-slate-800 text-white py-12 px-6'>
  <div class='container mx-auto mb-10 text-center'>
    <div>
      <h1 class='text-3xl md:text-5xl font-extrabold tracking-tight'>
        Sectores — RE+ México
      </h1>
      <p class='text-sm text-white/70 mt-1'>
        Explora soluciones y tecnologías en energía renovable e infraestructura.
      </p>
    </div>
  </div>
  <div class='max-w-6xl mx-auto grid gap-6 md:grid-cols-2 lg:grid-cols-3'>
    <!-- Card 1 -->
    <div class='relative rounded-2xl overflow-hidden shadow-lg'>
      <!-- Imagen de fondo -->
      <div
        class="absolute inset-0 bg-[url('/img/segmentos2.webp')] bg-cover bg-center"
      >
      </div>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/90'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <span class='text-4xl font-bold text-purple-300'>01</span>
          <h3 class='mt-4 text-xl font-semibold'>
            Sistemas de tecnología fotovoltaica
          </h3>
          <p class='mt-2 text-sm text-gray-200'>
            Paneles, inversores y BOS para generar electricidad desde un techo y
            gran escala.
          </p>
        </div>
      </div>
    </div>

    <!-- Card 2 -->
    <div class='relative rounded-2xl overflow-hidden shadow-lg'>
      <!-- Imagen de fondo -->
      <div
        class="absolute inset-0 bg-[url('/img/segmentos3.webp')] bg-cover bg-center"
      >
      </div>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/90'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <span class='text-4xl font-bold text-orange-400'>02</span>
          <h3 class='mt-4 text-xl font-semibold'>Almacenamiento</h3>
          <p class='mt-2 text-sm text-gray-200'>
            Baterías, BESS y software para gestión de energía, regulación y peak
            shaving.
          </p>
        </div>
      </div>
    </div>

    <!-- Card 3 -->
    <div class='relative rounded-2xl overflow-hidden shadow-lg'>
      <!-- Imagen de fondo -->
      <div
        class="absolute inset-0 bg-[url('/img/segmentos4.webp')] bg-cover bg-center"
      >
      </div>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/90'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <span class='text-4xl font-bold text-purple-300'>03</span>
          <h3 class='mt-4 text-xl font-semibold'>Componentes y equipo</h3>
          <p class='mt-2 text-sm text-gray-200'>
            Cables, conectores, seguidores, estructuras y protecciones.
          </p>
        </div>
      </div>
    </div>

    <!-- Card 4 -->
    <div class='relative rounded-2xl overflow-hidden shadow-lg'>
      <!-- Imagen de fondo -->
      <div
        class="absolute inset-0 bg-[url('/img/segmentos5.webp')] bg-cover bg-center"
      >
      </div>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/90'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <span class='text-4xl font-bold text-orange-400'>04</span>
          <h3 class='mt-4 text-xl font-semibold'>
            Tecnologías térmicas y solares
          </h3>
          <p class='mt-2 text-sm text-gray-200'>
            Calentamiento de agua, proceso térmico y solar de concentración.
          </p>
        </div>
      </div>
    </div>

    <!-- Card 5 -->
    <div class='relative rounded-2xl overflow-hidden shadow-lg'>
      <!-- Imagen de fondo -->
      <div
        class="absolute inset-0 bg-[url('/img/segmentosred.webp')] bg-cover bg-center"
      >
      </div>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/90'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <span class='text-4xl font-bold text-purple-300'>05</span>
          <h3 class='mt-4 text-xl font-semibold'>Infraestructura de red</h3>
          <p class='mt-2 text-sm text-gray-200'>
            Transporte de electricidad en redes inteligentes.
          </p>
        </div>
      </div>
    </div>

    <!-- Card 6 -->
    <div class='relative rounded-2xl overflow-hidden shadow-lg'>
      <!-- Imagen de fondo -->
      <div
        class="absolute inset-0 bg-[url('/img/segmentos6.webp')] bg-cover bg-center"
      >
      </div>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/90'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <span class='text-4xl font-bold text-orange-400'>06</span>
          <h3 class='mt-4 text-xl font-semibold'>
            Información, comunicación y servicios
          </h3>
          <p class='mt-2 text-sm text-gray-200'>
            Redes de comunicación y sistemas inteligentes de monitoreo.
          </p>
        </div>
      </div>
    </div>

    <!-- Card 7 -->
    <div class='relative rounded-2xl overflow-hidden shadow-lg'>
      <!-- Imagen de fondo -->
      <div
        class="absolute inset-0 bg-[url('/img/segmentos7.webp')] bg-cover bg-center"
      >
      </div>

      <!-- Overlay degradado -->
      <div
        class='absolute inset-0 bg-gradient-to-b from-[#2a2440]/70 to-[#1a1630]/90'
      >
      </div>

      <!-- Contenido -->
      <div class='relative p-6 flex flex-col justify-between h-full text-white'>
        <div>
          <span class='text-4xl font-bold text-purple-300'>07</span>
          <h3 class='mt-4 text-xl font-semibold'>Eólico e hidrógeno verde</h3>
          <p class='mt-2 text-sm text-gray-200'>
            Turbinas y tecnologías de hidrógeno para generación y almacenamiento
            limpio.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
